# KuroNexus — Kod Yapısı Denetimi · Parça 1/4

**Tarih:** 2026-09-01 · **Kapsam:** `frontend/` (Next.js 15 App Router) + `backend/` (NestJS 11 + Prisma 7) · **Mod:** salt okuma, sıfır kod değişikliği.

**Yöntem:** Özel yazılmış statik analiz betikleri (import-grafiği tabanlı ölü dosya taraması, değer-exportu erişim analizi, CSS Modules class eşleme, i18n anahtar taraması, bağımlılık kullanım sayımı) + hedefli dosya okumalarıyla doğrulanmış beş paralel tarama (duplicate frontend/backend, hardcoded frontend/backend, rota envanteri). Her bulgu dosya+satır kanıtlı; kanıtlanamayanlar "Belirsiz" işaretli. `src/generated/` (Prisma, git-ignored), `content/` ve `lib/characters|anime` altındaki küratöryel veri metinleri bilinçli içerik olarak denetim dışıdır.

**Depo ölçümü:** el yazımı kaynak 1.420 dosya / 455.661 satır (frontend 1.242 + backend 178). Üretilmiş Prisma istemcisi ayrıca 94 dosya / 173.398 satır (git'te takipli değil, derleme yükü olarak var).

---

## ÖZET

| Alan | Sonuç |
|---|---|
| Tamamen ölü dosya | **30** (frontend 29, backend 1) — ~12.030 satır |
| Ölü değer exportu (fonksiyon/sabit) | **22** (frontend 17, backend 5) |
| Kullanılmayan CSS class adayı | **72** (18 canlı modülde; 3 modül dinamik erişim → belirsiz) |
| Kullanılmayan i18n anahtarı | **~15 × 2 dil** (yüksek güven; 44 aday dinamik kullanım nedeniyle elendi) |
| Erişilmeyen rota adayı | **1** frontend sayfası + **7** hiç referanssız backend ucu |
| Kullanılmayan runtime dependency | **0** (iki "görünürde kullanılmayan" paket peer/generated zorunluluğu) — devDeps'te 2 aday |
| Duplicate kod | Backend'de sistemik (movies↔shows ekseni ~%80 ikiz), frontend'de ~1.300 satır silinebilir tekrar |
| Hardcoded içerik | 1 Critical (CORS fail-open), 1 Critical (compose'da varsayılan DB şifresi), gerisi config-drift/i18n sınıfı |

---

# 1. ÖLÜ KOD

## 1.1 Tamamen ölü dosyalar

### F-1 · `.deprecated` karakter deneyimleri — 24 dosya, 11.510 satır (413 KB)
- **Dosya:** `frontend/components/character/.deprecated/{kento-nanami, megumi-fushiguro, nobara-kugisaki, suguru-getou}/` (her klasörde Experience.tsx + 3-4 yardımcı bileşen + data.ts + module.css)
- **Satır kanıtı:** import-grafiği taramasında 24 dosyanın tamamı hiçbir giriş noktasından erişilemiyor; ör. `RatioExperience.tsx` importer sayısı 0. Ayrıca dördü kırık import taşıyor: `*/data.ts → ./types` çözülemiyor (dosya yok) — yani bu kod derlenmeye çalışılsa da kırılırdı.
- **Problem:** Depoda duran, hiçbir yerden import edilmeyen eski sürüm deneyimler.
- **Etki:** **Medium** (bundle'a girmiyor; maliyet bakım/arama gürültüsü + tsconfig `include: **/*.ts` kapsamında tip denetimi yükü)
- **Neden problem:** Bu 4 karakterin yeni sürümleri `components/character/{kento-nanami,...}` altında canlı; eskiler grep sonuçlarını kirletiyor ve editor/tsserver belleğine giriyor.
- **Çözüm:** Klasörü tamamen sil (git geçmişi zaten koruyor) ya da en azından `tsconfig.json` `exclude` listesine al.
- **Getiri:** ~11.5K satır daha az tip denetimi/indeksleme; grep hijyeni. Runtime/bundle kazancı yok (zaten dışarıda).

### F-2 · `HallSkeleton` + `AnimeHallSkeleton` — bilinçli emekli edilmiş ama silinmemiş iskeletler
- **Dosya:** [HallSkeleton.tsx](frontend/components/hall/HallSkeleton.tsx) (87 satır) + `HallSkeleton.module.css` (107), [AnimeHallSkeleton.tsx](frontend/components/anime/AnimeHallSkeleton.tsx) (75) + `AnimeHallSkeleton.module.css` (220)
- **Kanıt:** importer 0. Eski çağrı yerleri yorumla belgelenmiş: `app/[locale]/anime/(salon)/loading.tsx:24-27` "artık çağrılmıyor"; `app/[locale]/dark-stories/category/{kitap,film,dizi,anime}/arsiv/(dizin)/loading.tsx:8` "Burada HallSkeleton duruyordu. Kullanıcı kararı: sitede tek bir yükleme kimliği".
- **Etki:** **Low**
- **Neden problem:** Karar verilmiş (tek yükleme kimliği), kod emekli — ama dosyalar duruyor; yeni bir loading.tsx yazan biri yanlışlıkla bunları canlandırabilir.
- **Çözüm:** 4 dosyayı sil; loading.tsx'lerdeki tarihçe yorumları zaten kararı belgeliyor.
- **Getiri:** 489 satır; "hangi iskelet gerçek" karışıklığının sonu.

### F-3 · `app/[locale]/dark-stories/page.module.css` — sahipsiz stil dosyası
- **Dosya:** `frontend/app/[locale]/dark-stories/page.module.css` (29 satır: `title@L23`, `date@L30`, `cover@L35`, `content@L42` vb.)
- **Kanıt:** Aynı dizindeki `page.tsx`'in import listesi (satır 1-6) yalnızca `NexusHub` ve lib modülleri; module.css import'u yok.
- **Etki:** **Low**
- **Neden problem:** Sayfa `NexusHub`'a devredilmiş; CSS artığı yanıltıcı.
- **Çözüm:** Dosyayı sil.
- **Getiri:** 29 satır; dizin temizliği.

### B-1 · `backend/src/ambient-tracks/entities/ambient-track.entity.ts` — boş Nest schematic artığı
- **Dosya:** [ambient-track.entity.ts:1](backend/src/ambient-tracks/entities/ambient-track.entity.ts) — içeriği tek satır: `export class AmbientTrack {}`
- **Kanıt:** importer 0; gerçek model Prisma'da (`AmbientTrack` tablosu), servis Prisma tipini kullanıyor.
- **Etki:** **Low**
- **Çözüm:** Dosyayı ve boş `entities/` klasörünü sil.
- **Getiri:** Kozmetik; "entity nerede" sorusunun tek yanlış cevabı ortadan kalkar.

### Yanlış alarmlar (doğrulama şeffaflığı — silinmemeli)
- `frontend/lib/i18n/request.ts`: import-grafiğinde 0 importer görünür ama **canlıdır** — [next.config.ts:4](frontend/next.config.ts) `createNextIntlPlugin("./lib/i18n/request.ts")` string yoluyla yüklüyor.
- `frontend/styles/globals.css`: [layout.tsx:27](frontend/app/[locale]/layout.tsx) side-effect import'u ile canlı.
- `backend/prisma.config.ts`: Prisma CLI'nin config giriş noktası (Prisma 7 sözleşmesi) — canlı.

## 1.2 Ölü değer exportları (fonksiyon/sabit düzeyi) — **BÜYÜK ÖLÇÜDE KAPANDI (2 Eylül 2026 gecesi)**

Silinenler (tanım dosyası dışında sıfır kullanım, `-w` grep ile yeniden ölçüldü): `domainBySlug` (+`BY_SLUG`), `INNER_WORLD_IDS`, `CLAN_EMBLEM_IDS`, `VESTIGE_FIGURE_COUNT`, `musicHref.person`; backend `TFF_GS_CLUB_ID`, `TSDB_SUPER_LIG`, `AF_SUPER_LIG`, `SearchSpotifyDto`. **Bilerek kalanlar:** `GOJO_*` (dal açık), `RESERVED_*` üçlüsü (belge niteliğinde sözleşme), `curatedCharacterIds` (Itachi dosyasında planlanmış tüketici notu var), `isSortKey` (D-F1 ile birlikte), `AmbientTrack` (B-1). `/spor/futbol/oyuncu` sayfası ve `futbol` sözlük ad alanı F-4 ile ayrıca gitti.

Yöntem: yalnızca **değer** exportları (const/function/class) sayıldı; hiçbir dosyadan import edilmeyen VE kendi dosyası içinde de kullanılmayanlar listelendi. Tip exportları (bkz. 1.3) ve "içeride kullanılıyor, sadece `export` anahtarı fazla" olanlar burada değil.

**Frontend (17):**

| Dosya:satır | Export | Not / Şerh | Etki |
|---|---|---|---|
| `lib/characters/satoru-gojou-experience.ts:42,191,220,228,381,484,622,2083` | `GOJO_SITE_URL`, `GOJO_BLINDFOLD_TEXT`, `GOJO_ALT`, `GOJO_SECTIONS`, `GOJO_DETAILS`, `GOJO_POLE_UI`, `GOJO_CLOSING`, `GOJO_S11_SLOTS` (8 adet) | ⚠️ **Belirsiz:** `feat/gojo-satoru` dalı main'e merge edilmedi; bu sabitler o dalda kullanılıyor olabilir. Dal kapanmadan silinmemeli. | Low |
| `lib/anime/routes.ts:49` | `RESERVED_ANIME_SLUGS` | Rezerve slug sözleşmesi — kod okumuyor, belge niteliğinde. Silmek yerine bir denetim betiğine bağlanması daha değerli. | Low |
| `lib/music/routes.ts:55` | `RESERVED_ACT_SLUGS` | Aynı desen | Low |
| `lib/sport/routes.ts:61` | `RESERVED_CLUB_SLUGS` | Aynı desen | Low |
| `lib/characters/index.ts:25` | `curatedCharacterIds` | Gerçek ölü | Low |
| `lib/anime/jjk/domains.ts:197` | `domainBySlug` | Gerçek ölü fonksiyon | Low |
| `lib/anime/bleach/zanpakuto.ts:582` | `INNER_WORLD_IDS` | Gerçek ölü | Low |
| `lib/book/filters.ts:33` | `isSortKey` | Film/dizi ikizleri kullanılıyor, kitapta ölü — duplicate bulgusu 2.1-1 ile birlikte çözülür | Low |
| `components/anime/naruto/ClanEmblems.tsx:241` | `CLAN_EMBLEM_IDS` | Gerçek ölü | Low |
| `components/character/izuku-midoriya/MidoriyaGlyphs.tsx:182` | `VESTIGE_FIGURE_COUNT` | Gerçek ölü | Low |
| `lib/music/routes.ts` (`musicHref.person`) | rota yardımcısı | Rota envanteri bulgusu: `/muzik/kisi/[slug]` sayfası hiç yok, fonksiyon hiç çağrılmıyor (lib/music/routes.ts:36) | Low |

**Backend (5):**

| Dosya:satır | Export | Not | Etki |
|---|---|---|---|
| `src/ambient-tracks/entities/ambient-track.entity.ts:1` | `AmbientTrack` | = bulgu B-1 | Low |
| `src/football-live/providers/tff.provider.ts:56` | `TFF_GS_CLUB_ID` | Dışarıdan kullanılmıyor; hardcoded bulgu 3.2-11 ile ilişkili (kulüp profili tutarsızlığı) | Low |
| `src/football-live/providers/thesportsdb.provider.ts:40` | `TSDB_SUPER_LIG` | Aynı | Low |
| `src/football-live/providers/apifootball.provider.ts:41` | `AF_SUPER_LIG` | Aynı | Low |
| `src/music/dto/sync-spotify.dto.ts:37` | `SearchSpotifyDto` | Kullanılmayan DTO sınıfı | Low |

- **Çözüm (toplu):** Şerhsizler silinebilir; `RESERVED_*` üçlüsü ya bir check betiğine bağlanmalı ya yorum satırına indirilmeli; `GOJO_*` Gojo dalı kapanana kadar dokunulmamalı.
- **Getiri:** Küçük ama gerçek: tree-shaking bu sabitleri client bundle'dan her zaman atamıyor (yan etkisiz olduğu kanıtlanamayan modüllerde); asıl kazanç API yüzeyinin küçülmesi.

## 1.3 Dışarıdan hiç import edilmeyen exportlar (istatistik)

- **613 export / 247 dosya** dışarıdan hiç import edilmiyor (tam liste üretildi; büyük çoğunluğu `type`/`interface`). Bunlar ölü kod **değil** — çoğu kendi dosyasında kullanılıyor, yalnızca `export` anahtarı gereksiz.
- **Etki:** **Low** · **Neden:** Gereksiz `export`, "bunu dışarıda kullanan var mı?" sorusunu her seferinde pahalılaştırıyor; tip exportlarının runtime maliyeti sıfır.
- **Çözüm:** Toplu bir temizlik kampanyası önerilmez; dosyaya dokunulan her işte `export` anahtarı düşürülebilir. Otomasyon istenirse `knip` eklenmesi bu sayıyı sürekli izler.

## 1.4 Kullanılmayan CSS class'ları — **KAPANDI (2 Eylül 2026 gecesi).** Yeniden ölçüm (yorum ve `url()` soyulmuş, sınıflar yalnız seçiciden, `:global(...)` ve `composes:` canlı sayılmış, `styles[değişken]` okuyan 3 modül atlanmış): 19 dosyada 74 aday — raporun 72'si + D-F3'te `components/media`'ya taşınan fragman/kadro sınıfları (film ve dizi detayında 22). Silme kuralı: yalnız seçici listesindeki HER sınıfı ölü olan kural blokları (yapışık yorumuyla), `@media` içi dahil; canlı sınıfla karışık seçiciye dokunulmadı. Sonuç: 17 dosya, **669 satır saf silme**, satır sonları ve kalan yorumlar olduğu gibi. Kalan 8 aday kompozit seçicilerde (`.sectionHead .lede` gibi), zararsız. Kanıt: `next build`, `check:karakter`, `check:bleach` (bütçe), `check:jjk` temiz. Oyuncu sayfasının 20'si F-4 ile ayrıca gitmişti.

296 `.module.css` tarandı (importer'larındaki `styles.x` / `styles["x"]` kullanımına karşı); 3 modül dinamik erişim (`styles[değişken]`) nedeniyle **Belirsiz** sayıldı ve dahil edilmedi. Sonuç: **18 dosyada 72 ölü class adayı**. Bunlar F-1'dekilerin aksine **canlı dosyalarda** ve CSS Modules tüm dosyayı derlediği için **üretim CSS'ine giriyor**.

En yoğun dosyalar (adet / dosyadaki toplam class):

| Dosya | Ölü/Toplam | Ölü class'lar (satır) | Etki |
|---|---|---|---|
| `app/[locale]/spor/futbol/oyuncu/[playerId]/page.module.css` | **20/36** | `empty@111, sectionNote@138, tiles@147, tile@156, perfGrid@179, perfCard@185, perfLabel@194, perfValue@202, perfSub@209, meter@215, meterFill@220, cardsRow@233` +8 | **Medium** — dosyanın yarısından fazlası ölü; sayfanın kendisi de erişilmeyen rota adayı (bkz. 1.6) |
| `app/[locale]/muzik/[actSlug]/page.module.css` | 10/42 | `note@205, trackList@242, trackRow@249, trackNo@265, trackTitle@272, trackAlbum@279, discGrid@293, discCell@314, discLink@318, discTitle@325` | Medium — eski liste/diskografi düzeninin artığı |
| `components/character/CharacterSections.module.css` | 9/61 | `galleryEmpty@393, uploader*@527-600` (8 uploader class'ı) | Medium — uploader bloğu `CuratorUpload` bileşenine taşınmış görünüyor |
| `app/[locale]/muzik/tur/[genreSlug]/page.module.css` | 5/34 | `albumGrid@192, albumCell@214, albumLink@218, albumTitle@225, albumMeta@236` | Low |
| `app/[locale]/dark-stories/[universeSlug]/[storySlug]/page.module.css` | 4/6 | `title@23, date@30, cover@35, content@42` | Low — dosyanın 2 class'ı canlı |
| `components/layout/AccountMenu.module.css` | 4/19 | `tabs@43, tab@48, tabActive@49, profile@71` | Low |
| Kalan 12 dosya | 20 | `AkatsukiExhibit(3), CuratorUpload(3), naruto/page(2), BookCurator(2), CharacterDossier(2), GojoExperience(2), anime/layout(1), liste/[slug](1), jjk(1), court(1), StrawDoll(1), Orochimaru(1)` | Low |

- **Spot-check kanıtı:** `oyuncu/[playerId]` dizininde `tiles|perfGrid|meterFill|sectionNote` grep'i yalnızca `.module.css` tanımlarını döndürüyor; TSX kullanımı sıfır.
- **Neden problem:** Ölü class üretim CSS byte'ı + `composes`/refactor sırasında yanlış güven.
- **Çözüm:** Sayılan satırlar dosya bazında silinebilir; öncesinde her dosyada `styles[` dinamik erişim olmadığı zaten doğrulandı (dinamikler kapsam dışı bırakıldı). Oyuncu sayfası için 1.6'daki karardan sonra ele alınmalı.
- **Getiri:** ~72 class ≈ birkaç KB üretim CSS'i + stil dosyalarında gezinme hijyeni.

## 1.5 Kullanılmayan i18n anahtarları — **KAPANDI (2 Eylül 2026 gecesi):** aşağıdaki listedeki 16 anahtar (raporda 15 sayılmış) iki sözlükten de silindi. Öncesinde her biri için son-segment grep + sınırlayıcısız grep + dinamik `t(\`…${}\`)` kalıp taraması yeniden koşuldu; `akatsuki.labels.partner` için `labels.*` çağrılarının hepsi literal (`ring`, `dossier`) olduğu doğrulandı. `check:i18n` temiz, iki dil hâlâ tam senkron.

`messages/tr.json` = `messages/en.json` = **2.523 yaprak anahtar; iki dil tam senkron (0 eksik)** — olumlu bulgu. Son-segment korpus taramasıyla 59 aday çıktı; 44'ü dinamik anahtar kalıbı kanıtlanarak elendi (`t(\`roles.${...}\`)` → `muzik/[actSlug]/page.tsx:420`, `t(\`errors.${key}\`)` → `MusicCurator.tsx:125`, `t(\`treatment_${value}\`)` → `CuratedSlotEditor.tsx:832`, `t(\`${key}Short\`)` → `StatBars.tsx:56`, `t(\`state_${state}\`)` → `SlamDunkManifest.tsx:103`, `t(\`unit.${door.countUnit}\`)` → `page.tsx:297`).

**Yüksek güvenli ölü anahtarlar (15 × 2 dil):** `home.adminLink`, `stories.emptyUniverses`, `film.statTotal`, `film.statThisYear`, `film.statAverage`, `film.statNoRating`, `film.statWatchlist`, `book.lobby.openBook`, `account.profileTab`, `anime.bleach.meta.ogAlt`, `akatsuki.labels.partner`, `sportArchive.latest`, `sportArchive.club.personalArchive`, `music.embedTitle`, `music.embedNote`, `music.playlists.localPlayerNote` (16 oldu — `curator.state_default` ise **Belirsiz**: `state_${state}` dinamiği "default" değerini üretebilir).

- **Etki:** **Low** · **Neden:** `NextIntlClientProvider` üzerinden client'a taşınan mesaj yükünü şişirir; `film.stat*` beşlisi backend'e taşınan istatistik ekranının artığı görünümünde.
- **Çözüm:** Bu anahtarları iki JSON'dan da sil; öncesinde her biri için tam-yol grep'i (son segment değil) bir kez daha koşulmalı.
- **Getiri:** ~32 JSON satırı; sözlük bakım yükünde azalma.

## 1.6 Erişilmeyen rotalar ve çağrılmayan endpoint'ler

**Frontend: 143 page.tsx + 2 app/api route'u tarandı; 1 gerçek aday çıktı.**

### F-4 · `/[locale]/spor/futbol/oyuncu/[playerId]` — hiçbir yerden bağlantı verilmeyen sayfa
- **Dosya:** `frontend/app/[locale]/spor/futbol/oyuncu/[playerId]/page.tsx`
- **Kanıt:** `sportHref.player(` yalnızca sayfanın kendi metadata'sında (page.tsx:35); site içinde `<Link>`/`router.push` yok; sitemap'te yok. Tek gelen yol [next.config.ts:428-434](frontend/next.config.ts) eski adres 301'i. next.config yorumunun bahsettiği `SquadGrid` bileşeni artık depoda yok (grep 0) — yani bu sayfaya link veren bileşen bir refactor'da silinmiş, sayfa öksüz kalmış.
- **Etki:** **High** (bakım açısından: sayfa + 36 class'lık CSS'i (20'si zaten ölü, bkz. 1.4) canlı sanılıyor)
- **Neden problem:** Yalnızca eski dış linkler/tarayıcı geçmişiyle erişilebilen, siteden ulaşılamayan tam bir sayfa ağacı.
- **Çözüm (karar gerektirir):** Ya kadro kartlarından yeniden link verilmeli (kayıp `SquadGrid` işlevinin yerine geçen bileşene) ya da sayfa emekli edilip `/spor/futbol/futbolcular/[slug]` desenine 301 atılmalı.
- **Getiri:** Sayfa + CSS + veri çekme kodu netleşir; SEO'da öksüz sayfa sinyali biter.

Bilinçli bağlantısızlar (ölü değil): `/anime/bleach/playground` (tasarım deneme alanı, noindex — `sitemap.ts:46-48`), `/api/music-player` (iframe karantinası, `MusicPlayerBar.tsx:41` kullanıyor), `/api/dev-proxy` (dev aracı, `lib/api/client.ts:33,58`).

**Backend: 33 controller / ~194 endpoint çıkarıldı; ~177'sinin frontend çağrısı doğrulandı.** 8 uç yalnızca elle/script kullanımlı (curl operasyonları — `GET /health`, `PATCH /auth/password`, `POST /admin/books/awards/warm`, `POST|GET /admin/football/sync`, `POST|GET /admin/football/sync-league`, `POST /admin/music/albums/:spotifyId/refresh`, `GET /admin/music/genres/pending`, `GET /stories` — STATE.md/docs referansları mevcut, ölü sayılmaz).

### B-2 · Hiçbir yerden referans almayan 7 backend ucu
| Yöntem + Yol | Dosya:satır | Not | Etki |
|---|---|---|---|
| POST+GET `/admin/football/shirt-numbers` | `src/football/football.controller.ts:76,81` | Repo genelinde tek referans controller+service | Medium |
| GET `/universe-categories/:slug` | `src/categories/categories.controller.ts:16` | Frontend yalnız liste ucunu çağırıyor | Low |
| PATCH `/admin/movies/:id/refresh` | `src/movies/movies.admin.controller.ts:66` | Anime/kitap/müzik refresh'leri çağrılıyor; film çağrılmıyor | Low |
| PATCH `/admin/shows/:id/refresh` | `src/shows/shows.admin.controller.ts:81` | Aynı desen | Low |
| GET `/ambient-tracks/:id` | `src/ambient-tracks/ambient-tracks.controller.ts:46` | PATCH/DELETE kullanılıyor, GET kullanılmıyor | Low |
| GET `/transfer-news/:id` | `src/transfer-news/transfer-news.controller.ts:45` | Admin liste+POST+DELETE kullanıyor | Low |
| PATCH `/transfer-news/:id` | `src/transfer-news/transfer-news.controller.ts:51` | Düzenleme UI'si hiç yazılmamış (`lib/admin/api.ts`'te update yok) | Low |

- **Neden problem:** Her açık uç bakım + güvenlik yüzeyi; `refresh` uçları gibi bazıları muhtemelen UI'si unutulan işlevler (ölü uç mu, eksik UI mı — ürün kararı).
- **Çözüm:** `shirt-numbers` ve `transfer-news PATCH` için karar verilmeli (sil ya da UI bağla); diğerleri düşük öncelikli temizlik.
- **Getiri:** Küçük saldırı yüzeyi daralması + controller okunabilirliği.

## 1.7 Kullanılmayan hook / util özeti
- **Hook: 0 ölü.** `hooks/` tek dosya (`useDelayedVisible.ts`) ve kullanımda. (Ters bulgu: `useMotionSafe` genel işlevli olduğu halde `components/character/satoru-gojou/` içine hapsolmuş — bkz. 2.1-9.)
- **Util: 0 tamamen ölü util dosyası** (frontend `lib/` altındaki tüm dosyalar erişilebilir); ölü tekil fonksiyonlar 1.2'de.

---

# 2. DUPLICATE KOD

## 2.1 Frontend

### D-F1 · `lib/film/filters.ts` ↔ `lib/show/filters.ts` — dosyanın tamamı birebir kopya
- **Kanıt:** `SORT_KEYS` (film:11-19 ↔ show:9-17), `isSortKey` (25-27 ↔ 23-25), `PERIOD_OPTIONS` (34-54 ↔ 30-49), `matchesPeriod` (56-72 ↔ 51-67), `byNumber` (75-90 ↔ 69-84), `applyFilters` (92-134 ↔ 86-126). Tek fark parametre tipi. `lib/show/filters.ts:3-6` yorumu kopyayı itiraf ediyor. `byNumber`'ın üçüncü kopyası `lib/book/filters.ts:138-153`.
- **Etki:** **High** · **Neden:** Filtre davranışında yapılacak her düzeltme 2-3 dosyaya elle taşınmak zorunda; ayrışma sessiz olur.
- **Çözüm:** `lib/archive/filters.ts` altında jenerik `applyFilters<T>`; kitap en azından `byNumber`'ı oradan alsın.
- **Getiri:** ~130 satır silinir; tek doğruluk kaynağı.

### D-F2 · `FilmCurator.tsx` ↔ `ShowCurator.tsx` — ~%89 aynı (620 vs 581 satır, normalize diff 65 satır)
- **Kanıt:** `today()` (43-45 ↔ 37-39 birebir), `CuratorBar()` (48 ↔ 42, aynı state seti ve TMDB arama akışı), `CuratorCardTools()` (526 ↔ 488). Gerçek fark yalnızca dizi sezon/bölüm alanları.
- **Etki:** **High** · **Çözüm:** Ortak `components/curated/TmdbCuratorBar.tsx`; farklar prop'la. · **Getiri:** ~450 satır; admin küratör davranış birliği.

### D-F3 · `MovieDetail.tsx` ↔ `ShowDetail.tsx` — 10 alt parça ikiz
- **Kanıt:** `Trailer` (516-560 ↔ 836-880 birebir), `CastCard` (562-588 ↔ 882-908 birebir), `formatDate` (484-489 ↔ 819-824 birebir), `languageName` (501-509 ↔ 826-834 birebir), `LINK_FIELDS` (799 ↔ 1106 birebir); `ProviderChip`, `LinkRow`, `SimilarRow`, `QuickActions`, `ArchiveRow` yakın kopya.
- **Etki:** **High** · **Çözüm:** `components/media/` altına `Trailer/CastCard/ProviderChip`; sözlük ad alanı prop. · **Getiri:** ~250 satır.

### D-F4 · `getHall()` — 22 kopya
- **Kanıt:** 20 dosyada birebir aynı fonksiyon (tam liste ajan taramasında; örnekler: `app/[locale]/anime/(salon)/page.tsx:51`, `app/[locale]/dark-stories/category/kitap/page.tsx:49`, `.../kitap/arsiv/[shelf]/page.tsx:44` …) + 2 kısaltılmış ikiz (`spor/page.tsx:42`, `muzik/(salon)/page.tsx:63`).
- **Etki:** **High** (en mekanik, en düşük riskli kazanım) · **Çözüm:** `lib/halls.ts`'e tek `getHall(slug, fallbackName, locale)` — `hallLabel/hallName` zaten orada. · **Getiri:** ~290 satır.

### D-F5 · Küratör görsel yardımcıları `clamp/safe/slugify/serialize` — 3-4 kopya
- **Kanıt:** bleach `CuratedImage.tsx:295-338` ↔ jjk `CuratedImage.tsx:203-238` ↔ slam-dunk `CourtImage.tsx:275-311` ↔ `HallArt.tsx:185-209`; `clamp`+`safe` birebir. Slot kayıt kalıbı da üçüz: `lib/anime/{bleach,jjk,slam-dunk}/slots.ts` içinde `slotDef/defaultRatio/resolveRatio/slotsBySection` (bleach:706-744 ↔ jjk:228-253 ↔ slam-dunk:236-273; `defaultRatio`/`resolveRatio` birebir).
- **Etki:** **Medium** · **Çözüm:** `lib/curated/contract.ts` zaten "evrenden bağımsız sözleşme" — yardımcılar + `createSlotRegistry<TSection>` fabrikası oraya. · **Getiri:** ~200 satır; yeni evren açılışında tek satırlık kayıt.

### D-F6 · `lib/api/movies.ts` ↔ `lib/api/shows.ts` — ikiz API modülleri
- **Kanıt:** `TMDB_IMAGE_BASE` (13 ↔ 18), `tmdbImage` (22-29 ↔ 24-31 birebir), `EMPTY_*` sabitleri, `get*Showcase` (47-55 ↔ 40-48, `revalidate: 86400` dahil), `get*Archive` (64-70 ↔ 54-60). `shows.ts:22` yorumu "ikizinde yazılı" diyor.
- **Etki:** **Medium** · **Çözüm:** `lib/api/tmdb.ts` (görsel yardımcıları) + `fetchWithFallback<T>` genel yardımcı. · **Getiri:** ~60 satır + fallback davranış birliği.

### D-F7 · `shelves.ts` kalıbı — 4 kanat
- **Kanıt:** `shelfFromSlug` film:26-29 ↔ show:33-36 ↔ book:36-39 birebir; anime:39-41 sıkıştırılmış aynı ifade; `shelfHref` dördünde yalnız kanat adı farklı. (`belongsTo` kanada özgü — ortaklaştırılmamalı.)
- **Etki:** **Low** · **Çözüm:** `createShelves(keys, slugs, basePath)` fabrikası.

### D-F8 · Küçük yardımcı kopyaları
| Yardımcı | Kopyalar | Durum |
|---|---|---|
| `today()` | `FilmCurator.tsx:43`, `ShowCurator.tsx:37`, `BookCurator.tsx:55`, `admin/film/page.tsx:25` | 4 birebir |
| `languageName` | `MovieDetail.tsx:501`, `ShowDetail.tsx:826`, `SourceBook.tsx:294` (+ `BookHall.tsx:1132` sözlük tabanlı **çelişen** varyant) | 3 birebir + 1 çelişki (Belirsiz: kasıt mı) |
| `formatDuration` | `muzik/listeler/page.tsx:41`, `muzik/liste/[slug]/page.tsx:71`, `muzik/(salon)/page.tsx:71` (+2 varyant, bkz. 3.1-B1/B2) | 2 birebir + davranış farkı |
| `formatDate` | `MovieDetail.tsx:484`, `ShowDetail.tsx:819` (+ `BookDetail.tsx:528` medium-style varyant) | 2 birebir |
| `isTypingTarget` | `satoru-gojou/{DiscoveryProvider:79, VoidTrigger:32, SixEyesToggle:31}` | Aynı klasörde 3 birebir |
| `initials` | `BookCard.tsx:316`, `ReadingOrderHall.tsx:585` | 2 birebir (`toLocaleUpperCase("tr")` dahil) |
| `personHref` | `BookDetail.tsx:27`, `BookCard.tsx:80`, `SourceBook.tsx:33` | 3 tanım; tüketiciler rastgele birinden import ediyor |
- **Etki:** **Medium** (toplamda) · **Çözüm:** `lib/text.ts` / `lib/date.ts` / `lib/book/routes.ts` genişletmeleri. · **Getiri:** ~150 satır + davranış birliği.

### D-F9 · `prefers-reduced-motion` — 17 elle çağrı, hook dururken
- **Kanıt:** `window.matchMedia("(prefers-reduced-motion: reduce)")` 17 dosyada (`BackToTop.tsx:45`, `AnimeHall.tsx:491`, `HeroParallax.tsx:40`, `ScrollStrip.tsx:232`, `Reveal.tsx:47`, `HubStage.tsx:65`, `EyesPanel.tsx:92` …); tam bu işi yapan `useMotionSafe` hook'u `components/character/satoru-gojou/useMotionSafe.ts:40-48` içinde gömülü.
- **Etki:** **Medium** · **Neden:** Bazı kopyalar tek seferlik okuyor, bazıları değişiklik dinliyor — erişilebilirlik davranışı sayfa sayfa farklı.
- **Çözüm:** `hooks/useReducedMotion.ts` olarak genelleştir; 17 çağrı kademeli göç etsin. (Benzer: `new IntersectionObserver` kalıbı 14 dosyada — seçenekler farklı olabileceği için **Belirsiz**, tek tek incelenmeli.)

**Bilinçli tekrarlar (dokunulmamalı):** `pick`/`Localized` üç evren tip dosyasında (`lib/anime/jjk/types.ts:18-20` "bilinçli kopya" diye belgeli); karakter deneyimi bileşenlerinin şablon benzerliği (karakter sistemi kuralı).

## 2.2 Backend

Backend'in duplicate sorunu tek eksende sistemik: **movies ↔ shows modülleri neredeyse tam ikiz**, ikinci eksen "her modül kendi yardımcini yeniden yazıyor".

### D-B1 · `suggestions()` movies ↔ shows — ~100 satır birebir
- **Kanıt:** `movies.service.ts:318-418` ↔ `shows.service.ts:333-426`; round-robin karıştırma döngüsü (386-417 ↔ 395-425) tek karakter farksız; destek sabitleri (`SUGGESTION_SEEDS` vb., 20-28 ↔ 31-35) aynı.
- **Etki:** **High** · **Çözüm:** `src/common/tmdb/suggestion-mixer.ts` + saf `interleave()` (şu an test edilemez, testi de yok). · **Getiri:** ~100 satır + test edilebilirlik.

### D-B2 · `BookCoverService` ≡ `MusicArtworkService` — 268'er satırlık tam servis klonu (SSRF savunma hattı!)
- **Kanıt:** `SIGNATURES` (58-80 ↔ 61-78), `download()` (108-188 ↔ 121-192), `request()` redirect döngüsü (190-237 ↔ 207-252), `isAllowedHost()` (253-261 ↔ 253-261 **birebir**), `detectExtension()` (263-267 ↔ 263-267 **birebir**). Fark yalnız `ALLOWED_HOSTS`, dizin adı, `MAX_REDIRECTS`.
- **Etki:** **High** — bu bir güvenlik bileşeni; birinde yapılan yamanın diğerine geçmemesi gerçek risk.
- **Çözüm:** `createImageDownloader({subdir, allowedHosts, maxRedirects})` fabrikası; `SIGNATURES`+`isAllowedHost`+`detectExtension` derhal `src/common/media/`'ya (sıfır davranış farkı). · **Getiri:** ~250 satır + güvenlik yamalarının tek noktadan geçmesi.

### D-B3 · `TmdbService` ≡ `TmdbTvService` — HTTP+cache katmanı kopya
- **Kanıt:** `request<T>()` (378-413 ↔ 462-491, fark yalnız hata öneki), `cachedList()` (327-373 ↔ 423-460), `isBearerToken()`/`pickTrailer()`/`pickProviders()` birebir (416-418↔494-496, 470-478↔573-581, 486-509↔584-607).
- **Etki:** **Medium** · **Çözüm:** ortak `TmdbHttpClient` + `tmdb-mappers.ts`.

### D-B4 · movies ↔ shows kalan ikizler (tek kalemde)
`showcase()` (454-517 ↔ 450-512), `buildLinks/readCustomLinks/youtubeKey` (747-791 ↔ 960-1004; fark yalnız `/movie/`↔`/tv/`), `buildStats/topDirectors/allGenres` (821-871 ↔ 1085-1133), `buildNeighbours` (708-736 ↔ 932-957), `dedupe/shuffle` (651-673 ↔ 884-906 birebir Fisher-Yates), controller+DTO klonları (`dismiss-suggestion.dto.ts` **bayt bayt aynı**; admin controller'lar aynı 8 rota).
- **Etki:** **Medium-High** (toplamda ~300+ satır) · **Çözüm:** yapısal-tip jenerikleri (`buildWatchStats<T>` vb.) + `src/common/dto/`.

### D-B5 · `withSlugs`/`buildUniqueSlug` — 10 yeniden yazım, davranışları ayrışmış
- **Kanıt:** `withSlugs` 4 modülde (movies:684-698 ↔ shows:914-928 birebir; books:1798-1814, anime:1161-1170 varyant); `buildUniqueSlug` 6 yerde (stories:256-276 ↔ universes:140-161 ↔ wiki:149-171 birebir iskelet; **categories:108-127 `counter=1` ile başlıyor — ilk çakışmada diğerlerinden farklı sonek üretir**; music-playlist:321-334 200'de exception; music-sync:983-1005 500'de `Date.now()` soneki).
- **Etki:** **High** — aynı kural (AGENTS.md 14) dört farklı davranışla uygulanmış; slug üretimi URL sözleşmesinin kalbi.
- **Çözüm:** `src/common/utils/unique-slug.ts`; `music-sync`'teki callback'li imza doğru soyutlama, oraya taşınsın. · **Getiri:** Davranış birliği (satırdan önemlisi).

### D-B6 · `normalizeUrl` — 4 tanım, birinde bilinçli düzeltme geriye taşınmamış ⚠️
- **Kanıt:** movies:118-124 ↔ shows:97-103 ↔ books:1724-1730 birebir; **anime:1291-1300 farklı** — `startsWith('/')` kontrolüyle yerel `/uploads/...` yollarını koruyor. Diğer üçü `/uploads/x.jpg` girdisini `https:///uploads/x.jpg`'e çevirir.
- **Etki:** **High** — kopyalar arasında düzeltme kaybı; potansiyel hata (elle yapıştırılan yerel yol bozulur).
- **Çözüm:** Anime sürümünü `src/common/utils/normalize-url.ts`'e taşı, üç tanımı sil.

### D-B7 · `externalCache` kalıbı — **BAŞLADI (2 Eylül 2026 gecesi): soyutlama + TMDB kanadı bitti, kalan kanatlar sıradaki oturuma.**
- **Soyutlama:** `src/common/cache/external-cache.service.ts` (`ExternalCacheService`, global `ExternalCacheModule`): `read` / `readFresh(key, ttl)` / `write(key, payload)` / `remember(key, ttl, fetcher, { onStale, staleOnError })`. `remember` kural 4/14'ün kodu: taze → dön; değilse çek → yaz → dön; çekim düşerse bayat varsa `onStale` + bayat, yoksa hata. 7 birim testi (sahte Prisma). `write` iki dalda da `fetchedAt: now` — football-live'daki eksik `fetchedAt` sapması bu taşınınca kendiliğinden kapanır.
- **Taşınan:** `movies/tmdb.service.ts` (getMovie, cachedList) ve `shows/tmdb-tv.service.ts` (getShow, seasonEpisodes, cachedList) — 5 okuma/yazma çifti; anahtar, TTL ve bayata-düşme birebir korundu, `seasonEpisodes`'ın "kayıt yokken boş liste" yedeği dış `try` ile durdu. `PrismaService` bu iki servisten çıktı.
- **AniList + Jikan kanadı da taşındı (2 Eylül gecesi, 2/n):** `remember` iki genişleme aldı — TTL fonksiyon olabilir (`(hit) => ms`: yayını süren yapımda 6 saat, biteninde 7 gün, karar cache'teki `status`a bakar) ve `shouldWrite(payload)` (Jikan boş listeyi, AniList `null` karakteri cache'lemez). `getMedia` artık `getMediaWithRelations`'ın üstünde (aynı anahtar/görüntü); `getCharacters`/`getCharacterCards`/`getCharacter`/`episodes` bayat kayıt varsa `remember` onu sunuyor, kayıt yokken dış `catch` eski yedeği ([] / null) veriyor. 9 birim testi.
- **Kalan (sıradaki oturum, kanat başına bir commit):** `google-books.service.ts` (4), `bin-kitap.service.ts` (2), `awards.service.ts` (3), `spotify.service.ts` (4), `musicbrainz.service.ts` (2), `football.service.ts` (7), `football-live.service.ts` (2), `anime.service.ts`/`anime.cron.ts`/`shows.service.ts`/`movies.service.ts`/`books.service.ts` (liste/sayaç okumaları — `findMany`/`count`/`deleteMany` olanlar Prisma'da kalır, kalıp değil). Kural: davranışı birebir koru; canlıda ilgili uç deploy öncesiyle kıyaslanır.
- **Kanıt:** upsert kopyaları `anilist.service.ts:729-735`, `awards.service.ts:775-781`, `bin-kitap.service.ts:673-681`, `google-books.service.ts:733-744`, `football-live.service.ts:1159-1167` (**sonuncusunun `create` bloğunda `fetchedAt` eksik** — sessiz sapma); `Date.now() - cached.fetchedAt.getTime() < ttlMs` 22 ayrı yerde.
- **Etki:** **High** — repodaki en yaygın tekrar; cache tutarlılığı tek soyutlamayla çözülür.
- **Çözüm:** `src/common/cache/external-cache.service.ts` → `readFresh/readStale/write/swr`. · **Getiri:** 22 nokta tek davranışa iner; "bayata düş" kalıbı test edilebilir olur.

### D-B8 · Küçük kopyalar
`findByIdOrFail` (anime:1059, books:1477, movies:623, shows:855), `sleep` ×4 (jikan:143, bin-kitap:1096, football-live:1189, tff.provider:238 — birebir), `slugKey` ×2 (bin-kitap:1092, google-books:958), `assertGenreExists` ×2 (music-curator:517, music-playlist:297), `TURKISH_CHAR_MAP` kopyası (`common/utils/slugify.ts:1-15` kanonik ↔ `gs-official.provider.ts:150-169` aynen), `parseDate/parseNumber` çiftleri (football:891/897 ↔ books:1715, thesportsdb:178).
- **Etki:** **Medium** (toplamda) · **Çözüm:** `src/common/utils/{sleep,parse}.ts` + slugify export genişletmesi.

---

# 3. HARDCODED İÇERİK

## 3.1 Frontend

**Genel durum temiz:** 196 bileşen dosyası çeviri altyapısını kullanıyor; TSX'te `style={{color:"#..."}}` deseni 0; ham `fetch(` yalnızca 5 çağrı ve 5'i de merkezî `apiFetchUrl`/proxy üzerinden (`lib/api/client.ts:90`, `lib/admin/api.ts:539,1175,1230`, `app/api/dev-proxy/[...path]/route.ts:48`).

### H-F1 · Süre etiketi "sa"/"dk" 5 sayfada gömülü — İngilizcede Türkçe görünüyor (i18n'e taşınmalı)
- **Kanıt:** `` `${hours} sa ${minutes} dk` `` → `muzik/(salon)/page.tsx:78`, `muzik/[actSlug]/[albumSlug]/page.tsx:102`, `muzik/listeler/page.tsx:47`, `muzik/liste/[slug]/page.tsx:77`; **beşinci varyant farklı biçimle**: `muzik/dinleme/page.tsx:69` `` `${hours}s ${minutes}d` ``.
- **Etki:** **High** (kullanıcıya görünen dil hatası + aynı sitede iki farklı süre biçimi) · **Çözüm:** `messages/*.json`'a `music.duration` + tek `lib/music/format.ts` yardımcısı (D-F8 ile birlikte). · **Getiri:** EN sayfalarda doğru dil; 4 kopya fonksiyon silinir.

### H-F2 · `PaginatedReader.tsx` — genel okuyucuda gömülü TR (i18n'e taşınmalı)
- **Kanıt:** `components/story/PaginatedReader.tsx:257-258` `title/aria-label="Parşömen Modu"`, `:288` `title="Sayfaya gitmek için tıklayın"`. Bileşen diğer etiketleri prop'la alıyor — yarım bağlanmış.
- **Etki:** **Medium** (erişilebilirlik metni EN'de Türkçe) · **Çözüm:** üç metni prop ya da `useTranslations`'a al.

### ~~H-F3~~ · Rota literal'leri — **KAPANDI (2 Eylül 2026 gecesi; kitap ayağı 1 Eylül'de).** `lib/film/routes.ts` (`filmHref`) ve `lib/show/routes.ts` (`showHref`) açıldı; `animeHref` süpürüldü. Betikli süpürme (kuru çalışma + inceleme + uygulama): 36 dosya, 34 film/dizi + ~35 anime literali yardımcıya döndü, 33 dosyaya import eklendi; `.hall()`e dönen dört yer de `<Link>` (ikisi 301 yiyen eski `/dark-stories/category/anime` kökü — artık `/anime`). Kalan eşleşmeler yalnız yorum satırları ve bilinçli merkezler (`app/sitemap.ts`, `next.config.ts`). Müzik `PlaylistRail` `pathname.includes` kıyasları bu turda dokunulmadı (davranış kıyası, adres üretimi değil).
- **Kitap:** rota kurucular ≥8 dosyaya dağınık; **`KITAP_HREF` üç dosyada tanımlı ve İKİ FARKLI DEĞERDE** — `SourceBook.tsx:26` ve `PersonHall.tsx:25` `"/dark-stories/category/kitap"`, `ArchiveIndex.tsx:21` `"/dark-stories/category/kitap/arsiv"`. `lib/book/routes.ts:13-17` yalnızca `awardHref` içeriyor.
- **Anime:** SSOT var (`lib/anime/routes.ts`) ama süpürülmemiş — dosyanın kendi yorumu (10-12. satır) TODO diyor; 16 dosyada elle `/dark-stories/category/anime/...` (ör. `AnimeCard.tsx:107`, `CharacterDossier.tsx:134,209,419,510`).
- **Film/Dizi:** routes modülü hiç yok; ~20 dosyada literal (`MovieCard.tsx:66`, `ShowHall.tsx:181,234,324,335` …). `lib/halls.ts:199-222` `MOVED_HALLS` deseni salonların taşındığını gösteriyor — taşınma günü bu 20 dosya elle düzeltilecek.
- **Müzik:** `PlaylistRail.tsx:40,50,60` `pathname.includes("/muzik/tur")` vb. — `musicHref` dururken.
- **Toplam istatistik:** `/dark-stories` dizesi 154 dosyada 274 geçiş (tırnak içi gerçek literal: components ~77 + app 69 satır; next.config 40 ve sitemap 18 bilinçli merkez).
- **Etki:** **High** · **Çözüm:** `lib/film/routes.ts` + `lib/show/routes.ts` aç; kitap routes'u genişlet; anime süpürmesini TODO'dan çıkar; aynı adlı çelişen `KITAP_HREF`'leri derhal tekle. · **Getiri:** Bir sonraki salon taşınması tek dosyada biter; sessiz kırılma riski düşer.

### H-F4 · URL sabitleri
- **`TMDB_IMAGE_BASE` iki tanım** (`lib/api/movies.ts:13` ↔ `lib/api/shows.ts:18`) — taşınmalı (D-F6 ile aynı hamle).
- **YouTube embed/thumb adresleri 5 noktada** (`MovieDetail.tsx:531,547` ↔ `ShowDetail.tsx:851,867`, `PlayerFilm.tsx:75`) — [next.config.ts:139](frontend/next.config.ts) CSP'siyle elle senkron; tek yardımcıya alınmalı. **Medium**.
- **`http://localhost:3001` fallback'i 3 yerde** (`lib/api/client.ts:2`, `next.config.ts:6`, `layout.tsx:300`) — bilinçli (yorumla belgeli), **sadece not**: biri değişirse sessiz ayrışır.
- Dış atıf linkleri (themoviedb atıf, Spotify iframe API, commons) — **yerinde doğru**, taşınmasın.

### H-F5 · Renkler — sorun yok (not düzeyi)
TSX içi ham hex yalnızca 7 dosyada 21 adet, tamamı sanatsal SVG/dönem paleti (`ClanEmblems.tsx` 8, `HistoryReel.tsx:41-49` gerekçesi belgeli, …). Tek not: `app/manifest.ts:21-22` `"#101116"` = `styles/globals.css:226` `--hub-ink` kopyası — dosya içi yorum senkron zorunluluğunu belgeliyor; **sadece not**.

### H-F6 · Kalan TR metinler (karar gerektirir / not)
- ~~**`app/[locale]/anime/naruto/page.tsx` hiç çeviri kullanmıyor**~~ → **BİLİNÇLİ İSTİSNA OLARAK BELGELENDİ (2 Eylül 2026 gecesi, kullanıcı kararı "C").** Ölçüm: `/en/anime/naruto` kelimelerinin %33'ü Türkçe (Bleach/JJK'de %0.1); Bleach deseni (`Localized` veri + sözlük) burada ~15 KB lore metninin İngilizce yeniden yazımı demek, ~2 KB başlık/lede değil. O yazarlık işi yapılana kadar İngilizce adres dürüstçe "Türkçe kayıt": içerik kökü `lang="tr"`, `/en` adresi `noindex`, `shareCard({turkishOnly})` ile canonical/hreflang TR'ye kilitli, `og:locale` tr_TR, site haritasında `/en` eşi yok (`TURKISH_ONLY_PATHS`), sayfa üstünde tek satırlık İngilizce not (`anime.naruto.turkishOnly`). Çeviri yapılınca kaldırılacaklar: `turkishOnly` bayrağı (sayfa + sitemap seti), `noindex`, not. Gömülü başlıklar ve `page.tsx`teki Türkçe metin bilerek yerinde.
- Admin-içi gömülü TR (**sadece not**, kullanıcıya sızmıyor): `RichTextEditor.tsx:299-307,439` (font adları, "Rün Ayracı"), `NarutoPinEditor.tsx:246`.
- `app/manifest.ts:17-18` description yalnız TR — PWA kurulumunda EN kullanıcıya TR açıklama; **Low, sadece not**.

### H-F7 · Liste bekçiliği riskleri (sadece not)
- `DoorWall.tsx:37-45` `ATMOSPHERE_KEYS` salon listesini yeniden sıralıyor ve `HALL_ORDER`'dan silinmiş `kadim-dunyalar`'ı hâlâ içeriyor (fallback var, zararsız).
- `lib/anime/routes.ts:49-58` yorumu, iki slug'ın rezerv listesine yazılmayı unutulduğunu belgeliyor — rota açılışı ile rezerv listesini aynı yerde zorlayan desen düşünülmeli.

## 3.2 Backend

**Genel durum:** beklenenden temiz — JWT secret fallback'i YOK (`auth.module.ts:13` `getOrThrow`), 133 exception'ın tamamı i18n anahtarı, `.env` git geçmişine sızmamış (doğrulandı: `git ls-files` yalnız `.env.example`).

### H-B1 · CORS fail-open — `CORS_ORIGIN` unutulursa her origin'e credential'lı erişim ⚠️
- **Kanıt:** [main.ts:81-84](backend/src/main.ts) `origin: process.env.CORS_ORIGIN?.split(',') ?? true` + `credentials: true`. Koddaki yorum (78-80) "üretimde daraltılmış olmalı" diyor ama kod zorlamıyor. `sameSite:'lax'` kısmi azaltıcı.
- **Etki:** **Critical** · **Çözüm:** `NODE_ENV==='production'` iken `CORS_ORIGIN` boşsa boot'ta `getOrThrow` ile hata; fallback `true` olmamalı. · **Getiri:** Deploy'da tek unutulmuş env'in oturum çerezini tüm internete açması riski kapanır.

### H-B2 · `docker-compose.yml` — varsayılan Postgres şifresi + host'a açık port ⚠️
- **Kanıt:** [docker-compose.yml:8](docker-compose.yml) `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-kuronexus_de*****}` (maskelendi) + `ports: "${POSTGRES_PORT:-5432}:5432"`.
- **Etki:** **Critical** (dosya dev amaçlıysa **High**) · **Çözüm:** varsayılanı kaldır (değişken zorunlu olsun); binding `127.0.0.1:5432:5432`.

### H-B3 · Apify token'ı URL query'sinde
- **Kanıt:** `football.service.ts:539` `...?token=${this.apifyToken}&timeout=180`. Şu an loglanmıyor (hata mesajı yalnız status kodu) ama stack trace/proxy loglarına sızmaya kırılgan; Apify `Authorization: Bearer` destekliyor.
- **Etki:** **Medium (güvenlik notu)** · **Çözüm:** header'a taşı.

### H-B4 · Config drift dörtlüsü (config'e taşınmalı)
| Bulgu | Kanıt | Etki |
|---|---|---|
| Çerez ömrü sabit 24s, JWT ömrü env'den — `JWT_EXPIRES_IN=7d` yazıldığı an oturum davranışı bozulur | `auth-cookie.ts:24` `MAX_AGE_MS = 24*60*60*1000` ↔ `auth.module.ts:15-18` | **High** |
| Upload sınırı: kod 50 MB, `.env.example` 10 MB — env unutulursa sınır sessizce 5× açılır | `uploads.service.ts:19` ↔ `.env.example:29` | **High** |
| `.env.example`'da 14 env eksik (33 okunuyor / 21 örnekte); ikisi gizli anahtar (`SPOTIFY_CLIENT_SECRET`, `GOOGLE_BOOKS_API_KEY`) — yeni ortamda salonlar sessizce boş açılır | `.env.example` ↔ kod taraması | **Medium** |
| Env şema doğrulaması yok — üstteki üçünün kök nedeni | `app.module.ts:33` `ConfigModule.forRoot({isGlobal:true})`, `validationSchema` yok | **High (yapısal)** |
- **Çözüm:** `ConfigModule`'e `validate` fonksiyonu; çerez `maxAge`'i `JWT_EXPIRES_IN`'den türet; varsayılanları eşitle; `.env.example`'ı tamamla.

### H-B5 · Diğer config adayları
- `TSDB_API_KEY` varsayılanı `'3'` (halka açık test anahtarı) — `football-live.service.ts:215`; anahtar yoksa sağlayıcı kapanmalı, sahte anahtarla sürünmemeli. **Medium**.
- Global throttle sabit — `app.module.ts:35` `{ttl:60_000, limit:100}`; env'e alınmalı. (Auth'taki `@Throttle` 5/60sn **yerinde doğru** — brute-force sınırı env ile gevşetilememeli.) **Low**.
- `'./uploads'` varsayılanı 4 dosyada tekrar (`app.module.ts:42`, `uploads.service.ts:30`, `book-cover.service.ts:89`, `music-artwork.service.ts:95`) — tek sabit/provider. **Medium**.
- Kulüp/lig kimlikleri yarı env yarı sabit: `TFF_GS_CLUB_ID='3604'` (tff.provider:56), `TSDB_SUPER_LIG='4339'` (thesportsdb.provider:40), `TM_TEAM_ID` vars. `'141'` (football.service:169), `API_FOOTBALL_TEAM_ID` vars. `'645'` (football-live.service:241) — `FBL_CLUB_*` env sözleşmesinin dışında kalmışlar; kulüp değişse TFF/TSDB hâlâ Galatasaray çeker. **Medium** — tek `clubs.config.ts` profili.
- Apify actor kimliği sabit (`football.service.ts:40`). **Low**.

### H-B6 · Yerinde doğru olanlar (taşınmamalı — sadece not)
Dış API taban adresleri (~30 host; parse kodu o sürüme bağlı, env'e almak SSRF yüzeyi açar); SSRF host allowlist'leri (`book-cover.service.ts:29-40`, `music-artwork.service.ts:42-47` — güvenlik listesi env ile genişletilememeli); `BCRYPT_ROUNDS=12` (auth.service:15); cron ifadeleri (6 adet, hepsi UTC, kotaya göre kaydırılmış); Dockerfile'daki sahte `DATABASE_URL` (ayrı build aşamasında, runtime imajına taşınmıyor — doğrulandı); 104 isimlendirilmiş modül sabiti (`RELATED_LIMIT=12` sınıfı — isimlendirilmiş olmaları zaten doğru pratik). MIME listeleri iki yerde ve farklı (`uploads.service.ts:8-17` ↔ `remote-image.service.ts:36-41`; `image/avif` yalnız uzaktan kabul) — birleştirme tutarlılık kazandırır, **not**. i18n hata anahtarı kataloğu yok (yazım hatası derlemede yakalanmıyor) — ileride `as const` sözlüğü değerli, **not**.

---

# 4. SAYIMLAR

## 4.1 Toplam ölü kod

| Kategori | Adet | Ayrıntı |
|---|---|---|
| Ölü **dosya** | **30** | Frontend 29: `.deprecated` 24 + `HallSkeleton` 2 + `AnimeHallSkeleton` 2 + `dark-stories/page.module.css` 1 · Backend 1: `ambient-track.entity.ts` |
| Ölü **component** | **18** | `.deprecated` içinde 16 TSX bileşeni + `HallSkeleton` + `AnimeHallSkeleton` |
| Ölü **hook** | **0** | `useDelayedVisible` kullanımda |
| Ölü **util dosyası** | **0** tam dosya | Fonksiyon düzeyinde 22 ölü değer exportu (frontend 17, backend 5 — şerhler 1.2'de) |
| Ölü **CSS** | 3 tam `.module.css` (+ `.deprecated` içindeki 4) ve canlı dosyalarda **72 ölü class** (18 dosya) | 3 modül dinamik erişim → Belirsiz |
| Ölü **i18n anahtarı** | ~15-16 × 2 dil | 1 Belirsiz (`curator.state_default`) |
| Erişilmeyen **rota** | 1 sayfa adayı (`/spor/futbol/oyuncu/[playerId]`) | + bilinçli bağlantısız 1 (`bleach/playground`) |
| Çağrılmayan **backend ucu** | 7 hiç referanssız | + 8'i yalnız script/doc'tan (canlı operasyon uçları) |
| **Toplam ölü satır** | **~12.100** | 11.510 (.deprecated) + 518 (iskeletler+css) + ~70 (tekil export/anahtar payı) |

## 4.2 Kullanılmayan dependency analizi (repo içi kullanım doğrulanmış)

**Frontend `dependencies` — kaldırılabilir: 0**

| Paket | Durum |
|---|---|
| `@tiptap/pm` | Kaynak kodda import 0 — **ama** `@tiptap/react` v3'ün peer dependency'si; package.json'dan silinirse kurulum bozulur. **Kaldırma adayı değil** (yalnızca "neden burada" yorumu hak ediyor). |
| `sharp` | Doğrudan import yalnız `scripts/build-bleach-og.mjs` — **ama** Next.js üretim görsel optimizasyonunun runtime motoru. Kalmalı. |
| `@tiptap/*` (5 uzantı + starter-kit + react) | `components/admin/RichTextEditor.tsx` + `lib/editor/loreMention.ts` — kullanımda |
| `next`, `next-intl`, `react`, `react-dom` | 283-330 dosyada — kullanımda |

**Frontend `devDependencies`:** tamamı araç zinciri (eslint ailesi, typescript, @types) — kullanımda.

**Backend `dependencies` — kaldırılabilir: 0**

| Paket | Durum |
|---|---|
| `reflect-metadata`, `rxjs` | Doğrudan import 0 — **ama** NestJS'in zorunlu peer'ları (decorator metadata + Nest iç Observable'ları). Kaldırılamaz. |
| `@prisma/client` | `src`'de import 0 görünür — **ama** üretilmiş istemci deep-import ediyor: `src/generated/prisma/client.ts:16` `from "@prisma/client/runtime/client"`. Kalmalı. |
| Kalan 18 paket | Tümünün import'u doğrulandı (`bcrypt` 2, `csv-parser` 2, `unzipper` 1, `helmet`/`compression` main.ts, `sanitize-html` 2 …) |

**Backend `devDependencies` — kaldırma adayı: 2**

| Paket | Kanıt | Etki |
|---|---|---|
| `ts-loader` | `nest-cli.json`'da webpack modu yok (tsc derliyor); repo genelinde referans 0 | Low — silinebilir, `npm run build` ile doğrula |
| `source-map-support` | Repo genelinde referans 0 (script/`-r` bayrağı dahil) | Low — silinebilir, prod start ile doğrula |

Diğer devDep'ler doğrulandı: `@nestjs/schematics` (nest-cli.json `collection`), `tsconfig-paths`/`ts-node` (package.json `test:debug` + prisma.config seed), `supertest`/`jest`/`ts-jest` (test zinciri), `prisma` (CLI).

**Fantom bağımlılık notu:** `express` 6 dosyada import ediliyor ama package.json'da yok — hepsi `import type` (ör. `app.controller.ts:3`), tipler `@types/express`'ten çözülüyor; runtime'a girmiyor. **Low** — yine de `@nestjs/platform-express`'in getirdiğine güvenmek yerine açık yazılması tartışılabilir.

## 4.3 En büyük 20 dosya (satır sayısı, el yazımı kod)

| # | Dosya | Satır |
|---|---|---|
| 1 | `frontend/components/character/satoru-gojou/GojoExperience.module.css` | 3.601 |
| 2 | `frontend/messages/en.json` | 3.121 |
| 3 | `frontend/messages/tr.json` | 3.121 |
| 4 | `frontend/components/anime/akatsuki/AkatsukiExhibit.module.css` | 2.549 |
| 5 | `frontend/app/[locale]/anime/naruto/page.module.css` | 2.275 |
| 6 | `backend/src/books/books.service.ts` | 2.190 |
| 7 | `frontend/components/character/kiba-inuzuka/KibaExperience.module.css` | 2.080 |
| 8 | `frontend/lib/characters/satoru-gojou-experience.ts` | 2.066 |
| 9 | `frontend/components/character/kenpachi-zaraki/KenpachiExperience.module.css` | 2.054 |
| 10 | `frontend/components/character/konan/KonanExperience.module.css` | 2.043 |
| 11 | `frontend/components/character/toshinori-yagi/PlusUltraExperience.module.css` | 2.016 |
| 12 | `frontend/components/character/yamato/YamatoExperience.module.css` | 1.990 |
| 13 | `frontend/components/character/choji-akimichi/ChojiExperience.module.css` | 1.952 |
| 14 | `frontend/components/character/ichigo-kurosaki/IchigoExperience.module.css` | 1.948 |
| 15 | `frontend/components/character/sukuna-itadori/VesselExperience.module.css` | 1.947 |
| 16 | `frontend/components/character/kankuro/KankuroExperience.module.css` | 1.929 |
| 17 | `frontend/components/character/neji-hyuga/NejiExperience.module.css` | 1.929 |
| 18 | `frontend/components/character/naruto-uzumaki/NarutoExperience.module.css` | 1.894 |
| 19 | `frontend/components/character/rock-lee/RockLeeExperience.module.css` | 1.890 |
| 20 | `frontend/components/character/kabuto-yakushi/KabutoExperience.module.css` | 1.884 |

Not: 20'nin 15'i karakter deneyimi CSS'i — karakter sayfası sisteminin bilinçli sonucu (sayfa başına bağımsız palet/dünya kuralı). En büyük **backend** dosyası `books.service.ts` (2.190) — 2.2'deki ortaklaştırmalar (slug, cache, parse) onu da küçültür. Üretilmiş Prisma istemcisi kapsam dışı tutuldu (94 dosya / 173.398 satır; en büyüğü `internal/prismaNamespace.ts` 8.638 satır — git'te takipli değil).

---

# 5. YÖNTEM NOTLARI, BELİRSİZLER, YANLIŞ ALARMLAR

- **Araçlar:** import-grafiği ve export analizi regex tabanlı özel betiklerle yapıldı (AST değil); side-effect import, dynamic `import()`, `@/` alias, index çözümü ve `next-intl` string-yolu istisnası elle doğrulandı. İlk turda iki yanlış pozitif üretildi ve giderildi (`styles/globals.css`, `lib/i18n/request.ts` — bkz. 1.1 "Yanlış alarmlar").
- **Belirsiz bırakılanlar:** `GOJO_*` exportları (merge edilmemiş dal), `curator.state_default` anahtarı, 3 dinamik-erişimli CSS modülü, `IntersectionObserver` 14 kopyasının seçenek eşitliği, `BookHall` sözlük-tabanlı `languageName` varyantının kasıt olup olmadığı, `/spor/formula-1/surucular/[slug]` linklerinin tamamen veri-bağımlı oluşu, `/admin/music/playlists` GET/POST satır-düzeyi yöntem eşlemesi.
- **Bilinçli olduğu belgeli ve dokunulmaması gerekenler:** üç evrende `pick/Localized` kopyası, SSRF allowlist'leri, dış API taban adresleri, auth `@Throttle` sabitleri, karakter CSS paletleri, `bleach/playground`.

**En yüksek getirili beş hamle (öneri sırası):** ① H-B1 CORS fail-open kapatma + env şema doğrulaması (Critical, ~1 saat), ② D-B2 SSRF indirici klonunun tekleştirilmesi (güvenlik yamaları tek noktadan geçsin), ③ D-F4 `getHall` ×22 → `lib/halls.ts` (mekanik, ~290 satır), ④ F-1 `.deprecated` silme (11.5K satır), ⑤ H-F3 film/show routes modülleri + çelişen `KITAP_HREF`'lerin tekleştirilmesi.
