# DEVİR NOTU — 11 Ağustos 2026 (Müzik Salonu)

> Bağlam penceresi dolduğu için yazıldı. **Yeni oturuma başlayan ajan bunu
> `STATE.md`'den hemen sonra okur.**
>
> Bir günde yapılan iş: Salon 06 · Müzik sıfırdan kuruldu ve canlıya çıktı;
> Kadim Dünyalar kapısı kaldırıldı; MusicBrainz ikinci kaynak olarak devreye
> girdi. Yolda **yarım saatlik bir üretim kesintisi** yaşandı — kaydı ve dersi
> aşağıda, atlanmaması gereken bölüm o.

---

## 1 · ŞU AN NE ÇALIŞIYOR (canlıda doğrulandı)

**Salon 06 · Müzik** — `https://kuronexus.com/muzik`

| Ölçüm | Değer |
| --- | --- |
| Arşiv | 1 sanatçı (Linkin Park) · 63 albüm · 610 parça |
| Kapaklar | Hepsi kendi diskimizde (`/uploads/music/…`), hotlink yok |
| Tür odası | 1 oda "Rock & Metal" (`accentKey: rock`) + 5 alt tür |
| Rotalar | `/muzik`, `/muzik/tur`, `/muzik/tur/rock`, `/muzik/[actSlug]`, `/muzik/dinleme` → 200 (TR + EN) |
| Hata yolları | olmayan sanatçı/tür 404 · geçersiz dönem 400 · admin kimliksiz 401 |
| Kapı duvarı | `01 FİLM · 02 DİZİ · 03 SPOR · 04 ANİME · 05 Kitap · 06 MÜZİK · 07 𐱅 Temürkan` |

Linkin Park künyesi: `actKind: BAND`, `formedYear: 1996`, köken "Agoura Hills,
United States" — **dördü de MusicBrainz'den**, Spotify hiçbirini vermiyor.

**Faz 6 bitti:** Kadim Dünyalar kategorisi yumuşak silindi, 6 boş evren
silindi (14 adet 301 yazıldı), `zaman-carki` kitap kategorisine geçti,
`temurkan-efsaneleri` kategorisiz kaldı ve mühürlü kapısı 07'de duruyor.

**Bugünün commit'leri** (`3a2889d` → `a160540`, 11 tane): mimari inceleme →
Faz 1 (şema+backend+4 ekran) → revert → düzeltilmiş yeniden uygulama → Faz 6
kod → Faz 6 SQL → üç Spotify/MusicBrainz düzeltmesi.

---

## 2 · SIRADAKİ İŞ (öncelik sırasıyla)

### 2.1 Küratör uçlarını bağla — ✅ BİTTİ (11 Ağustos, ikinci oturum)

Dördü de `music.admin.controller.ts`e eklendi ve karşılıkları
`frontend/lib/admin/api.ts`e yazıldı:

| Servis metodu | Uç | Ön yüz karşılığı |
| --- | --- | --- |
| `listGenres` | `GET /admin/music/genres` | `listMusicGenres` |
| `createGenre` | `POST /admin/music/genres` | `createMusicGenre` |
| `setActGenres` | `PUT /admin/music/acts/:id/genres` | `setMusicActGenres` |
| `updateAct` | `PATCH /admin/music/acts/:id` | `updateMusicAct` |

⚠️ `PATCH`/`PUT` uçlarındaki `:id` **arşiv kimliği** (cuid); kardeş uçlar
(`acts/:spotifyId/refresh`) Spotify kimliği alıyor. Karışırsa hata 404 olarak
görünür.

### 2.2 Küratör modu arayüzü — ✅ BİTTİ (11 Ağustos, ikinci oturum)

`components/music/MusicCuratorSwitch.tsx` (istemci adası, ~70 satır) +
`components/music/MusicCurator.tsx` (`next/dynamic`, mod açılınca iniyor).

**Kitap salonundan bilinçli AYRILDIĞIM yer:** orada kabuğun tamamı
(`BookHall`) istemci bileşeni. Müzik sayfaları saf sunucu bileşeni ve öyle
kaldı — içlerinde süzgeç/sıralama yok, hepsi tek istekten çizilen okuma
yolları. Bütün ağacı `"use client"` yapmak küratör düğmesi uğruna ziyaretçiye
salonu JavaScript olarak indirmek olurdu. Onun yerine sayfaya tek bir ada
girdi. Ölçüm: `/muzik` ilk yük **129 kB** (panel içinde değil).

- **Salon paneli** (`/muzik`): durum şeridi (Spotify bağlı mı, rol sözlüğü,
  başarısız sync'lerin `lastError`'ı), canlı sanatçı araması → ekle
  (arkasından MusicBrainz zenginleştirmesi otomatik), adres yapıştırma
  (sanatçı/çalma listesi), tür sözlüğü (oluştur · onayla · onayı kaldır),
  bakım (kapak indir · rol sözlüğü · dinleme bağla · geçmiş dosyası yükle).
- **Sanatçı paneli** (`/muzik/[actSlug]`): tür ataması (çipler, son hâl
  gönderiliyor) + künye (grup/solo, yıllar, köken, küratör notu, bant görseli
  yüklemesi) + "Spotify'dan tazele" ve "MusicBrainz'den zenginleştir".

**Arama neden yalnızca SANATÇI arıyor:** bir sanatçıyı eklemek diskografisinin
tamamını getiriyor (Linkin Park'ta 63 albüm, 610 parça). Albüm/parça araması
ayrı bir ekleme yolu açardı ve albümü sahipsiz bırakırdı — arşivde albüm her
zaman bir act'e bağlı. `syncAlbum` zaten yalnızca arşivdeki albümü tazeliyor.

**Hata metinleri sebebi TAŞIYOR:** backend anahtarı (`MUSIC.GENRE_SLUG_TAKEN`)
çevrilebiliyorsa çevrilmiş metin, çevrilemiyorsa anahtarın kendisi + HTTP kodu
gösteriliyor. §4.5'teki gözlemleme dersi arayüze de uygulandı.

### 2.2b Eski 2.2 pürüzü — ✅ ÇÖZÜLDÜ, ama beklenenden farklı

Pürüz `router.refresh()` ile **kapanmıyordu**: sunucu bileşeni yeniden çalışsa
da `revalidate: 300` işaretli `fetch` yanıtı Next'in Data Cache'inden dönüyor.
`export const dynamic = "force-dynamic"` de bunu değiştirmiyor (yalnızca
`fetch` VARSAYILANINI `no-store` yapıyor; açıkça yazılmış `revalidate` onu
eziyor).

Çözüm: önbellek kaldırılmadı, **çağırana göre** ayrıldı. `lib/api/music.ts`in
dört getiricisi artık `fresh?: boolean` alıyor; sayfalar `readIsAdmin()`
sonucunu geçiriyor. Küratör `no-store` okuyor, ziyaretçi beş dakikalık
önbellekten. `readIsAdmin()` çerez yoksa istek bile yapmıyor, yani ziyaretçiye
maliyet çıkmıyor. Dört sayfada birden uygulandı (`/muzik`, `/muzik/tur`,
`/muzik/tur/[genreSlug]`, `/muzik/[actSlug]`).

### 2.2c Bu arayüzde HENÜZ OLMAYAN

- **Albüm/parça düzenlemesi.** Panelde albüm silme, parça gizleme, dönem
  (`MusicEra`) kurma yok — servis metotları da yok. Faz 3'ün işi.
- **Tür adının/anahtarının düzeltilmesi.** `updateMusicGenre` `name`, `key`,
  `accentKey` alanlarını kabul ediyor ama panel yalnızca onay düğmesi
  gösteriyor. Yanlış adla oluşturulan bir tür şu an panelden düzeltilemiyor
  (tarayıcı konsolundan `updateMusicGenre` çağrılabilir).
- **Çalma listesi silme.** Ekleme var, çıkarma yok.
- **Oda dizini/oda sayfasında küratör anahtarı.** Yalnızca `/muzik` ve
  `/muzik/[actSlug]` üzerinde. Tür işleri salon panelinden yürüyor.
- **Yerelde uçtan uca sınanMADI.** Derleme temiz (frontend `npm run build`,
  backend `nest build`, 5 müzik testi yeşil) ve tipler tutuyor, ama uçlar
  gerçek bir veritabanına karşı çağrılmadı — canlıda doğrulanacak
  (yönergesi §2.2d).

### 2.2d Canlıda ilk doğrulama sırası

Admin girişiyle `https://kuronexus.com/muzik` → "KÜRATÖR MODU":

1. **Durum şeridi** "Spotify bağlı" diyor mu? Demiyorsa Coolify →
   `kuronexus-backend` → Environment Variables eksik.
2. **Tür sözlüğü** dolu geliyor mu? (`GET /admin/music/genres` — bu oturumda
   eklenen uç; 404 dönerse deploy eski.)
3. **Yeni tür oluştur** → dizinde beliriyor mu? (`POST /admin/music/genres`)
4. Sanatçı sayfasında **tür ata → kaydet** → oda sayfasında sanatçı
   görünüyor mu? (`PUT …/genres` + önbellek ayrımı birlikte sınanıyor.)
5. **Künye kaydet** → kıvrımdaki çipler değişiyor mu? (`PATCH /admin/music/acts/:id`)

### 2.2e Salonun gezinme boşlukları — ✅ KAPATILDI (12 Ağustos)

Kullanıcı canlıda gezerken beş şey bildirdi; hepsi aynı kökten geliyordu:
**salon tek sanatçıya göre kurulmuştu.**

| Bildirilen | Kök sebep | Yapılan |
| --- | --- | --- |
| "Sanatçı kartı hep Linkin Park'ı açıyor" | Kart `overview.acts[0]`e bağlıydı | `/muzik/sanatcilar` dizini + kart oraya bağlandı |
| "Albümlere tıklayınca açılmıyor" | `musicHref.album` vardı, **rota dosyası yoktu** → 404 | `/muzik/[actSlug]/[albumSlug]` sayfası |
| "Kapaklar çok büyük" | Izgara 2/3/5 sütun | 3/4/6 sütun + ilk 12, kalanı "tümünü göster" |
| "Biyografi olsa güzel" | `bio` çalar kutusunun altında küçük paragraftı | Kendi bölümü oldu |
| "Çalma listeleri bölümü" | Yalnızca kavşaktaki favori şeridi vardı | `/muzik/listeler` + `/muzik/liste/[slug]` |

⚠️ `routes.ts`in başındaki söz ("rota dosyaları bu adreslerle birebir
eşleşir") tutulmamıştı ve kimse fark etmedi çünkü **hiçbir test adresi
gerçekten açmıyordu.** Bugün açık kalan iki adres daha var:
`musicHref.person` (`/muzik/kisi/:slug`) — rota yok, ama bugün hiçbir yerden
bağlanmıyor da.

### 2.2f Kendi çalma listelerimiz — ✅ (12 Ağustos)

**Migration GEREKMEDİ:** `MusicPlaylist.spotifyId` zaten nullable ve unique.
Kimliksiz satır tamamen geçerli ve `MusicSyncService` ona erişemiyor (listeleri
`where: { spotifyId }` ile buluyor). Kişisel katmanın sync'ten ayrı durması
şemadan bedavaya geldi.

- `music-playlist.service.ts` — kur, adlandır, sil (yumuşak), parça ekle/çıkar,
  sırala. Çıkarma sonraki sıraları kaydırıyor; sıralama iki aşamalı yazıyor.
- Uçlar: `GET/POST(local)/PATCH/DELETE /admin/music/playlists`,
  `POST|DELETE .../tracks`, `PUT .../tracks` (sıra).
- Arayüz: her parça satırında **+** (albüm · sanatçı · liste sayfaları),
  menüden mevcut listeye ekle ya da oracıkta yeni liste kur. Yalnızca **yerel**
  listeler seçilebiliyor — Spotify'dan gelene eklenen parça tazelemede
  kaybolurdu.

### 2.2g Site geneli çalar şeridi — ✅ (12 Ağustos)

Kullanıcı isteği: "müzik sayfasından ayrıldığımda diğer sayfalarda da
çalmaya devam etsin, ama ince bir şerit hâlinde."

- `components/player/MusicQueue.tsx` — kuyruk **kök düzende**. App Router'da
  gezinme kök düzeni yeniden mount etmiyor, iframe hayatta kalıyor. Kuyruk
  `localStorage`da; tam sayfa yenilemesi sesi keser ama şerit kaldığı parçayla
  geri gelir (çalmayı kendiliğinden BAŞLATMAZ).
- `components/player/MusicPlayerBar.tsx` — Spotify **IFrame API** ile kuyruk:
  `loadUri` + `playback_update`. Parça bitince kendiliğinden sıradakine geçiyor.
- 🔴 **CSP DEĞİŞTİ:** `script-src` artık `https://open.spotify.com` içeriyor.
  Sayfamızda dış kaynaklı bir betik çalışıyor. **Kullanıcıya soruldu ve
  onaylandı (12 Ağustos 2026).** `connect-src` genişletilMEDİ.
  Alternatifi elle "ileri"ye basmaktı — gömü iframe'i başka origin'de olduğu
  için parçanın bittiğini betik olmadan göremiyoruz.
- Betik yüklenmezse şerit **sessizce ölmüyor**: 10 sn'lik zaman aşımı var,
  uyarı yazılıyor, ileri/geri çalışmaya devam ediyor.
- Ortam sesi çalarıyla çakışmasın diye tek yönlü olay: müzik başlayınca
  `GlobalAmbientPlayer` duruyor.

⚠️ **Önizleme sınırı:** tarayıcıda Spotify oturumu Premium değilse gömü
parçanın ~30 saniyesini çalar. Bizim tarafımızda çözülebilecek bir şey değil.

### 2.2h Bu tur YERELDE UÇTAN UCA SINANDI

Önceki turun aksine ölçüm var (12 Ağustos, yerel PG 16 + `node dist/main` +
üretim derlemesi port 3100):

- 28/28 migration uygulandı, backend ayağa kalktı, **bütün yeni uçlar
  `RouterExplorer`da göründü.**
- Gerçek girişle (sahte çerez değil) liste akışının tamamı koşturuldu: kur →
  aynı adla ikinci liste `gece-listesi-2` slug'ı aldı → parça ekle (üçü) →
  aynı parçayı tekrar ekle `added:false` → sırala → çıkar (sıralar kaydı) →
  eksik dizi ile sırala `400 PLAYLIST_ORDER_MISMATCH`.
- `/music/acts/baska-grup/albums/deneme-album` → **404** (albüm yanlış
  sanatçının adresi altında açılmıyor).
- Tarayıcıda: "Listeyi çal" → şerit çıktı → **ana sayfaya gezindi, şerit ve
  kuyruk aynı bileşen olarak yaşamaya devam etti** → "Sonraki" ilerledi ve
  kuyruğun sonunda kendini kapattı. `spotifyId`i olmayan parça kuyruğa
  girmedi ("Listeyi çal (2)", 3 parçalık albümde).
- ⚠️ Spotify betiği bu makinede yüklenemedi (ağ dışarı çıkmıyor), yani
  **otomatik geçiş canlıda doğrulanacak.** Kuyruk/şerit/gezinme kısmı
  betikten bağımsız ve doğrulandı.

### 2.3 Dinleme geçmişi içe aktarımı → 2d ekranı

Kullanıcı Spotify'dan **"Extended streaming history"** talebini başlattı
(11 Ağustos). Dosya e-postayla gelecek (birkaç gün). Geldiğinde:

`POST /admin/music/listening/import` (multipart `file`) — zip içindeki JSON
dosyaları **tek tek** yüklenir. Tekrar yüklemek güvenli
(`@@unique([userId, playedAt, spotifyTrackUri])`). Sonra
`POST /admin/music/listening/backfill` ile dinlemeler arşivdeki parçalara
bağlanır.

⚠️ **Spotify Web API'sinde çalma sayısı ucu YOK.** 2d ekranındaki her sayı bu
dosyaya bağlı. `/me/top/*` sıralama verir sayı vermez;
`/me/player/recently-played` yalnızca son 50 kaydı verir.

### 2.4 Kalan kuyruk maddeleri

- **Site metinleri**: `stories.wingAncient` "Evrenler" oldu, üç manşet cümle
  B stratejisiyle güncellendi. `home.tagline` bilinçli olarak dokunulmadı
  (oradaki "kadim" salonun adı değil, bilgiyi niteleyen sıfat).
- **7 test suite'i BENDEN ÖNCE kırık**: `books/*` (jest transform hatası),
  `categories.*` ve `app.controller` (Nest CLI iskeletleri, `PrismaService`
  sağlanmamış). Dokunulmadı. ⚠️ Çoğu kırmızı olan bir küme, yeni testlerin
  kırılmasını da gizler.
- **GitHub 34 bağımlılık güvenlik açığı bildiriyor** (24 yüksek). Ayrı tur.
- **`Dockerfile` `CMD` zinciri** — bkz. §4.4, karar bekliyor.

---

## 3 · ÖLÇÜLEN GERÇEKLER (tahmin değil — yeniden keşfetmeyin)

### 3.1 Spotify sanatçı nesnesi SADELEŞTİRİLMİŞ

Canlı `ExternalCache` kaydından `jsonb_object_keys` ile ölçüldü:

```
GELEN:    external_urls, href, id, images, name, type, uri
GELMEYEN: genres, popularity, followers
```

Sonuçları:
- **Tür taksonomisi Spotify'dan HİÇ gelmiyor** → MusicBrainz'den geliyor
- **`popularity`/`followers` her zaman null** → hiçbir sıralama onlara
  dayanmıyor (üç sıralama albüm sayısına çevrildi, rozet metni "albüm
  sayısına göre" oldu)

Yeni bir alana güvenmeden önce aynı ölçümü yapın:
```sql
SELECT string_agg(k, ', ') FROM "ExternalCache",
  jsonb_object_keys(payload::jsonb) k WHERE "cacheKey" = 'spotify:artist:…';
```

### 3.2 Spotify sorgu dizesi tuzakları

| Yapılan | Sonuç |
| --- | --- |
| `searchParams.set('include_groups','album,single,compilation')` → `%2C` | **400** |
| Düz virgül | 200 |
| `limit=50` (belgede üst sınır 50!) | **400 "Invalid limit"** |
| `limit` hiç gönderme + `next` adresini izle | 200 |

**Kodda artık hiçbir uca `limit` gönderilmiyor.** `paged()` ilk sayfayı
parametresiz istiyor ve sonraki sayfaları Spotify'ın verdiği `next` adresini
**aynen** izleyerek alıyor. `buildUrl()` virgülleri korunur biçimde kuruyor ve
`spotify.service.spec.ts` bunu 5 testle kilitliyor.

### 3.3 MusicBrainz

- Anahtar gerekmiyor, ücretsiz. **Tanıtıcı `User-Agent` zorunlu** (yoksa 403).
- ⚠️ **1100 ms yetmedi.** Sürecin attığı ilk iki istek bile 503 aldı; sebebi
  muhtemelen paylaşımlı Hetzner IP'si. Şimdi **1500 ms + geri çekilmeli
  tekrar** (2s/5s/12s, `Retry-After` öncelikli).
- Eşleştirme **Spotify adresinden** (`/url?resource=…&inc=artist-rels`) —
  kesin eşleşme. Ada göre arama yedek ve yalnızca **puan ≥ 90** kabul.
- Verdikleri: `genres` (oy sayılı), `type`→`actKind`, `life-span`→yıllar,
  `area`/`begin-area`→köken. Vermedikleri: popülerlik/takipçi.
- MusicBrainz türleri de **onay kapısından geçiyor**: Linkin Park için altı
  tür geldi (rock, metal, nu metal, rap metal, rap rock, alternative rock) ve
  yalnızca biri oda oldu, beşi `parentId` ile alt tür yapıldı.

### 3.4 Altyapı

- **Yerel PostgreSQL 16 kurulu:** `K:\postgres\pgsql\bin`. Docker Desktop
  **YOK** ve gerekmiyor. Başlat:
  `pg_ctl -D "K:\postgres\data" -l "K:\postgres\server.log" -w start`
  (bu oturumda açık bırakıldı). Bağlantı: `K:\postgres\LOCAL_DB_URL.txt`
- ⚠️ `backend/.env` `DATABASE_URL` **ÜRETİMİ** gösteriyor
  (`65.108.220.5:5433`) ve evden erişilemiyor. Yerel test için `DATABASE_URL`i
  kabukta ver (dotenv mevcut değişkeni ezmiyor) ve **her seferinde
  `prisma migrate status` ile hangi veritabanına bağlandığını doğrula.**
- Backend konteynerinde **`curl` YOK** (alpine) → `node -e` kullan.
  Postgres konteynerinde **`node` YOK** → `psql` kullan.
- ⚠️ **Coolify web terminali uzun/çok satırlı yapıştırmayı bozuyor** —
  satırın ortasına devam istemi (`>`) sokuyor ve dizeyi kırıyor. Komutları
  kısa ve tek satır tut.

---

## 4 · KESİNTİ KAYDI VE DERSLER (atlamayın)

### 4.1 Ne oldu

`2d30a73` push edildi → `prisma migrate deploy` **P3009** ile patladı →
`CMD` zincirli olduğu için (`migrate deploy && node dist/main`)
`node dist/main` hiç çalışmadı → backend crash-loop → **bütün API yaklaşık
yarım saat 503**. Ön yüz ayakta kaldığı için ziyaretçi "arşive ulaşılamıyor"
ekranı gördü.

**Kök sebep:** migration dosyasının 1. satırı SQL değildi:
`Loaded Prisma config from prisma.config.ts.` — yani Prisma CLI'nin banner'ı.
Dosya `... --script > migration.sql 2>&1` ile üretilmişti; `2>&1` stderr'i
dosyaya kattı. PostgreSQL: `42601 syntax error at or near "Loaded"`.

**`git revert` YETMEDİ:** bozuk satır `_prisma_migrations` tablosundaydı,
`migrate deploy` kodda migration olmasa da aynı hatayı verdi. Çözüm o satırı
silmek oldu.

### 4.2 Denetim neden yakalamadı

Dosya push'tan önce incelenmişti — ama **aranan şeyler** arandı: `CREATE TABLE`
sayısı, `ALTER` hedefleri, FK'ler, 63 karakter sınırı, tekrar eden index
adları. Hiçbiri "bu dosya geçerli SQL ile başlıyor mu" diye sormadı. Satır
sayısının 401→496 çıktığı görüldü ve 95 satırın tamamı yeni tablolara yazıldı;
biri çöptü.

### 4.3 Alınan önlemler

1. Migration artık **kabuk yönlendirmesiyle üretilmiyor**:
   `prisma migrate diff --output <dosya>`. Banner'ın sızması yapısal olarak
   imkânsız.
2. **`backend/scripts/check-migrations.mjs`** — her `migration.sql`in ilk
   anlamlı satırı SQL mi, dosyada CLI gürültüsü var mı diye sınıyor. Bozukta
   çıkış kodu 1. Bağımlılık yok.
3. **Migration gerçek PostgreSQL'de koşmadan push edilmiyor.** Bu oturumda
   iki senaryo da sınandı: boş DB'ye 28/28 **ve** 27'si uygulanmış DB'ye tek
   başına (üretimin gerçek durumu).

### 4.4 KARAR BEKLEYEN MİMARİ MADDE

`backend/Dockerfile` `CMD`: `npx prisma migrate deploy && node dist/main`

**Herhangi bir** migration hatası backend'i tamamen düşürüyor. Tek satırlık
bir hatayı yarım saatlik kesintiye çeviren şey bu zincir — hatanın kendisi
kadar bu tasarım da. Ayırmanın yolları var (migration'ı ayrı adım yapmak, ya
da hata durumunda uygulamayı yine başlatıp sağlık ucundan bildirmek) ama
deploy davranışını değiştirmek kendi riskini taşıyor. **Kullanıcı kararı.**

### 4.5 Tekrarlayan hata sınıfı — ders

Bir günde **üç kez** aynı sınıf hata: *kendi kurduğum bir değerin dış servis
tarafından kabul edileceğini varsaymak.*

1. `%2C` (virgül kodlaması) → 400
2. `limit=50` (belgedeki üst sınır) → 400
3. `1100 ms` (belgedeki hız sınırı) → 503

Üçünde de çözüm "doğru değeri bul" değil, **"o değeri hiç kurma"** oldu:
virgülü koru, `limit` gönderme (`next`i izle), sınıra dayanma (bekle+tekrar).

Ve **üç kez** aynı gözlemleme boşluğu: hata fırlatan dal log basmıyordu ya da
sebep `.catch(() => null)` ile yutuluyordu. Hepsi kapatıldı. **Kural: dış
servise giden her yolda hata sebebi çağırana kadar taşınır** — `matched:
false` yetmez, `reason` de dönmeli.

---

## 5 · KOMUTLARI NEREDE ÇALIŞTIRACAK

Kullanıcı amatör olduğunu söyledi ve komutun yerini iki kez sormak zorunda
kaldı. **Her kod bloğunun üstüne nerede koşacağını yaz.** Bu projede beş yer:

| Yer | Ne için |
| --- | --- |
| Coolify → `kuronexus-backend` → **Terminal** | `node -e`, ortam değişkenleri, `prisma` |
| Coolify → `postgresql-database-…` → **Terminal** | `psql`, SQL sorguları |
| Coolify → servis → **Environment Variables** | Anahtar eklemek ("available at buildtime" **işaretsiz**) |
| Coolify → servis → **Logs** | Hata teşhisi (Deployment Log DEĞİL) |
| **Tarayıcı konsolu** (admin girişli) | `/admin/*` uçları, `fetch(..., {credentials:'include'})` |
| Kendi bilgisayarı: **Git Bash / PowerShell** | Repo işleri |

---

## 6 · DOSYA HARİTASI

**Belgeler**
- `docs/kuronexus-muzik-bolumu-tasarim-plani.md` — kullanıcının planı
- `docs/muzik-bolumu-inceleme.md` — **mimari inceleme + uygulama komutu.**
  ⚠️ Planla çelişirse İNCELEME KAZANIR (plan `ultnexus`tan gelen Redis/Bull
  varsayımını ve bu şemada çakışan çıplak entity adlarını taşıyor)
- `backend/prisma/sql/remove-kadim-hall.sql` — Faz 6 göç SQL'i (koştu)

**Backend** (`backend/src/music/`, 13 dosya)
- `spotify.service.ts` — Client Credentials, `buildUrl`, `paged` (`next` izler)
- `musicbrainz.service.ts` — hız sınırlı, geri çekilmeli tekrar
- `music-sync.service.ts` — katalog upsert + `enrichFromMusicBrainz`
- `music-curator.service.ts` — tür oluştur/onayla, act künyesi (uçları yarım)
- `music.service.ts` — sayfaların okuduğu katman, 4 metot = 4 ekran
- `listening.service.ts` — geçmiş dosyası içe aktarma
- `music-artwork.service.ts` — kapak indirme (beyaz liste + SSRF)
- `music-roles.service.ts`, `music.cron.ts`, `music.module.ts`,
  `music.controller.ts`, `music.admin.controller.ts`, `dto/`
- `spotify.service.spec.ts` — 5 test (`%2C` regresyonu)

**Frontend**
- `app/[locale]/muzik/` — 8 rota + `layout` + `error` + `loading`
  (`/`, `/tur`, `/tur/[genreSlug]`, `/sanatcilar`, `/[actSlug]`,
  `/[actSlug]/[albumSlug]`, `/listeler`, `/liste/[slug]`, `/dinleme`)
- `components/music/` — `CoverArt`, `GenreMixBar`, `SpotifyEmbed`
- `components/music/MusicCuratorSwitch.tsx` — küratör anahtarı (istemci adası)
- `components/music/MusicCurator.tsx` — panel; `HallCurator` + `ActCurator`,
  `next/dynamic` ile tembel (ziyaretçiye inmiyor)
- `components/music/TrackList.tsx` — parça satırı: çal + listeye ekle
  (albüm · sanatçı · liste sayfalarının üçünde de aynı bileşen)
- `components/music/Discography.tsx` — 3/4/6 sütun + "tümünü göster"
- `components/music/PlaylistCreator.tsx` — liste dizinindeki "yeni liste"
- `components/player/MusicQueue.tsx` — site geneli kuyruk (kök düzende)
- `components/player/MusicPlayerBar.tsx` — ince şerit + Spotify IFrame API
- `lib/music/routes.ts` — **tek adres kaynağı** (`musicHref`), Spotify gömü
- `lib/api/music.ts` — 4 getirici + tipler
- `lib/admin/api.ts` — sonunda müzik küratör bölümü (10 fonksiyon)
- `styles/globals.css` — `[data-category="muzik"]` derisi + 4 oda accent'i

---

## 7 · DEĞİŞTİRİLMEYECEK KARARLAR

- **Salon 06 = Müzik, Salon 07 = Temürkan.** Temürkan `HALL_ORDER`'a ASLA
  eklenmez (o liste kategori slug'ı tutuyor; kategori olmayan bir slug bütün
  numaraları kaydırır). Kapısı `app/[locale]/page.tsx:135-150`'de elle
  ekleniyor.
- **Redis/Bull YOK.** `@Cron` + `MusicSyncState`. Kuyruk DB'de olduğu için
  `pg_dump` yedeğine giriyor.
- **Entity adları `Music*` önekli.** `model Role` şemayı derletmiyor
  (`enum Role` zaten var); `Track`/`Person`/`Genre` de çakışıyordu.
- **Kişisel katman ayrı tablolarda.** Sync bir gün `data: {...payload}` yazsa
  aynı satırdaki puan/not sessizce silinirdi.
- **Kapak hotlink YOK**, hepsi `/uploads/music/`. CSP `img-src` Spotify
  CDN'ine izin vermiyor ve dış adres bir gün ölüyor.
- **Tür onay kapısı kalıyor** (`isApproved: false`). İlk gerçek işini yaptı.
- **`accentKey` renk DEĞİL token anahtarı.** Bilinen liste:
  `rock`, `pop`, `rnb`, `electronic` (`globals.css`te blokları var).
- **Salon adı "Müzik" / "Music"**, yeni font eklenmedi (beşinin karşılığı
  evde vardı: Cinzel, Bebas Neue, Cormorant self-host + mono sistem yığını).
- **Tasarımdan bilinçli ayrılmalar** (hepsi kodda yorumlu): 2a alt şeridi
  "şu an çalan" değil **en son dinlenen**; 2c "oda payı" yerine parça sayısı;
  2d "yeni keşif" yerine toplam dinleme; 2b'de çalışmayan sıralama düğmeleri
  yerine aktif ölçü rozeti.

---

## 8 · AÇIK SORULAR

1. **Faz 2 (kişisel katman) öncesi:** İngilizce not zorunlu mu? (Öneri: hayır
   — TR yazılır, EN boş kalabilir, gösterimde dolu olana düşülür.)
2. **`Dockerfile` `CMD` zinciri** ayrılsın mı? (bkz. §4.4)
3. **Şarkı seviyesinde tür** ileride gerekecek mi? Bugün reddedildi (MB'de
   kayıt türleri seyrek); gerekirse `MusicGenreOnTrack` + migration.
