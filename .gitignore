.env
.claude/settings.local.json

# NOT: GUVENLIK-GUNLUGU.md başta buradaydı (zayıf noktaların zaman çizelgesi
# saldırgana yol gösterir diye). 4 Ağustos 2026'da depoya alındı: kritik
# açıklar kapandı, depo private ve 0 collaborator var, ve belgenin diğer
# makinelere ulaşması gerekiyor. Depo public yapılacaksa ÖNCE çıkarılmalı.

# Claude / agent skill dosyaları — araç yapılandırması, proje kodu değil.
# Diskte kalır, depoya girmez (7,5 MB / 361 dosya, üstelik ikisi birbirinin kopyası).
# NOT: .agents/AGENTS.md ve .claude/launch.json bilinçli olarak depoda kalıyor —
# ilki projenin talimat dosyası, ikincisi geliştirme sunucusu ayarı.
.agents/skills/
.claude/skills/
